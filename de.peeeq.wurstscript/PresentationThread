Introduction:
Wurstscript is a scripting language which can compile to jass code which is used in wc3.

The Compiler:
The WurstScript Compiler 

The language:
The WurstScript Syntax uses indention to define Blocks rather than keywords like vanilla Jass (expect packages, which use endpackage). Also WurstScript will make many more or less unneeded keywords from vanilla Jass like "set" and "call" optional. 

Every Codesegment in WurstScript has to be defined in a package

The optimizer:


Usage:
Java Runtime Eviroment 7 - http://www.oracle.com/technetwork/java/javase/downloads/index.html

Links & download:
Google Codepage - http://code.google.com/p/pscript-lang/
Downloads - http://code.google.com/p/pscript-lang/downloads/list
Wiki + Documentation - http://code.google.com/p/pscript-lang/w/list