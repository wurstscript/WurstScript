sudo: required
language: java

jdk:
  - oraclejdk8
  
before_install:
  - chmod +x gradlew
  - sudo apt-get -qq update
  - sudo apt-get install -y wine
  
before_script: cd de.peeeq.wurstscript

script:
  - ./gradlew assemble test --info