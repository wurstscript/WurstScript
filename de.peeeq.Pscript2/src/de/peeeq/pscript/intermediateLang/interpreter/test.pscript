package test {
	native type Int = integer
	native type Bool = boolean
	
	function foo(x: Int, y: Int):Int {
		while ( not (x > 0) or x <= 0 ) {
			y = y + 1
			x = x - 1
		}
		return y
	}
	
	
}