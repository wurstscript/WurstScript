package Hashtable
    
    function hashtable.saveReal( integer parentKey, integer childKey, real value ) returns real
        SaveReal( this, parentKey, childKey, value )
        return value

endpackage